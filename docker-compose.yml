version: '3.8'
services:
  db:
    image: postgis/postgis:15-3.3
    container_name: geoflow_db
    environment:
      - POSTGRES_USER=user
      - POSTGRES_DB=geodata
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - "5433:5432"
